services:
  postgres:
    image: postgres:16-alpine
    container_name: mde-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: mde
      POSTGRES_PASSWORD: mde
      POSTGRES_DB: medical_data
    ports:
      - "5434:5432"
    volumes:
      - mde_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mde -d medical_data"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Optional: a lightweight web UI for poking at the DB.
  # Browse to http://localhost:8081 and connect with system=postgres, server=postgres,
  # user=mde, password=mde, database=medical_data.
  adminer:
    image: adminer:latest
    container_name: mde-adminer
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8081:8080"
    profiles: ["ui"]

volumes:
  mde_pgdata:
